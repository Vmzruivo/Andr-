import fs from 'node:fs';
const f='codex-vitae.jsx';
let s=fs.readFileSync(f,'utf8');
if(s.includes('CODEX_MISSION_RELIABILITY_V2')){console.log('Mission reliability patch already applied');process.exit(0)}

// CODEX_MISSION_RELIABILITY_V2
s=s.replace(
  'deleteMyAccount } from "./src/lib/supabaseClient";',
  'deleteMyAccount, claimSystemMission } from "./src/lib/supabaseClient";'
);

const oldClaim='const claimMission=async m=>{if(!missionReady(m)||m.completed)return;const xp=(effectiveProfile.total_xp||0)+m.xp,li=levelInfo(xp);try{const p=await updateProgress(session.user.id,{total_xp:xp,level:li.level,quests_completed_ever:(effectiveProfile.quests_completed_ever||0)+1});setProfile(p);setMissions(x=>({...x,[m.difficulty]:{...m,completed:true}}));setMissionNotice(`Missão concluída: +${m.xp} XP`);setTimeout(()=>setMissionNotice(""),3500)}catch(e){setError(e.message)}};';
const newClaim='const claimMission=async m=>{if(!session||!missionReady(m)||m.completed||m.cancelled)return;setSaving(true);try{const p=await claimSystemMission(today(),m.difficulty,m.xp);setProfile(p);setMissions(x=>({...x,[m.difficulty]:{...m,completed:true,claimedAt:new Date().toISOString()}}));setMissionNotice(`Missão concluída: +${m.xp} XP`);setTimeout(()=>setMissionNotice(""),3500)}catch(e){setError(e.message)}finally{setSaving(false)}};\n const cancelMission=m=>{if(!m||m.completed||m.cancelled)return;if(!window.confirm(`Cancelar a missão "${m.title}"? Ela não poderá ser retomada hoje.`))return;setMissions(x=>({...x,[m.difficulty]:{...m,cancelled:true,cancelledAt:new Date().toISOString()}}));setMissionNotice("Missão cancelada. Você poderá escolher outra dificuldade.");setTimeout(()=>setMissionNotice(""),3500)};';
if(!s.includes(oldClaim))throw new Error('claimMission não encontrado');
s=s.replace(oldClaim,newClaim);

const oldCard='const activeMission=missions[difficulty]&&missions[difficulty].date===today()?missions[difficulty]:null;const mp=activeMission?missionProgress(activeMission):0;const missionCard=<div className="cv-card"><div className="cv-section-head"><div><h2>Missão do sistema</h2><p className="cv-muted">1 missão por dificuldade por dia. Você precisa fazer a ação no site.</p></div></div><div className="cv-switch" style={{margin:"14px 0",display:"grid",gridTemplateColumns:"repeat(5,1fr)"}}>{DIFFICULTIES.map(d=><button key={d.key} className={difficulty===d.key?"active":""} onClick={()=>setDifficulty(d.key)}>{d.label}<small>{d.xp} XP</small></button>)}</div>{!activeMission?<button className="cv-button" onClick={()=>missionFor(difficulty)}>Receber missão</button>:<div className="cv-quest"><div className="cv-quest-info"><b>{activeMission.title}</b><small>{activeMission.desc}</small><div className="cv-progress"><span style={{width:`${Math.min(100,mp/activeMission.target*100)}%`}}/></div><small>{mp}/{activeMission.target} · +{activeMission.xp} XP</small></div>{activeMission.completed?<Check/>:missionReady(activeMission)?<button className="cv-button" onClick={()=>claimMission(activeMission)}>Resgatar XP</button>:<span>Em andamento</span>}</div>}</div>;';
const newCard='const activeMission=missions[difficulty]&&missions[difficulty].date===today()?missions[difficulty]:null;const mp=activeMission&&!activeMission.cancelled?missionProgress(activeMission):0;const activeMissionCount=Object.values(missions).filter(m=>m&&m.date===today()&&!m.cancelled&&!m.completed).length;const missionCard=<div className="cv-card"><div className="cv-section-head"><div><h2>Missões do sistema</h2><p className="cv-muted">Você pode manter uma missão ativa por dificuldade. O progresso é atualizado conforme você realiza as ações no site.</p></div><b>{activeMissionCount}/5 ativas</b></div><div className="cv-switch" style={{margin:"14px 0",display:"grid",gridTemplateColumns:"repeat(5,1fr)"}}>{DIFFICULTIES.map(d=>{const mm=missions[d.key]&&missions[d.key].date===today()?missions[d.key]:null;return <button key={d.key} className={difficulty===d.key?"active":""} onClick={()=>setDifficulty(d.key)}>{d.label}<small>{mm?.completed?"✓ concluída":mm?.cancelled?"✕ cancelada":mm?`${missionProgress(mm)}/${mm.target}`:`${d.xp} XP`}</small></button>})}</div>{!activeMission?<button className="cv-button" onClick={()=>missionFor(difficulty)}>Receber missão de {DIFFICULTIES.find(d=>d.key===difficulty)?.label}</button>:activeMission.cancelled?<div className="cv-quest"><div className="cv-quest-info"><b>Missão cancelada</b><small>Você não poderá receber outra missão desta dificuldade hoje.</small></div><X/></div>:<div className="cv-quest"><div className="cv-quest-info"><b>{activeMission.title}</b><small>{activeMission.desc}</small><div className="cv-progress"><span style={{width:`${Math.min(100,mp/activeMission.target*100)}%`}}/></div><small>{mp}/{activeMission.target} · +{activeMission.xp} XP</small></div>{activeMission.completed?<Check/>:missionReady(activeMission)?<div><button className="cv-button" disabled={saving} onClick={()=>claimMission(activeMission)}>{saving?"Verificando...":"Resgatar XP"}</button><button className="cv-button" style={{marginTop:8}} onClick={()=>cancelMission(activeMission)}>Cancelar</button></div>:<button className="cv-button" onClick={()=>cancelMission(activeMission)}>Cancelar</button>}</div>}</div>;';
if(!s.includes(oldCard))throw new Error('missionCard não encontrado');
s=s.replace(oldCard,newCard);

fs.writeFileSync(f,s);console.log('Mission reliability patch applied');
